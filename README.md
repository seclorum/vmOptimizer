# vmOptimizer
A Windows Powershell script to inspect local Windows VM-related configuration options, as well as VirtualBox VM instances, and provide a report of things to change to get better VM performance in Virtualbox on Windows.
